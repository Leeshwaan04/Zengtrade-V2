.env
__pycache__/
*.pyc
.venv/
venv/
*.log
.DS_Store

# data dumps
*.csv

# runtime state & scratch (not source)
paper_state.json
paper_state.bak*
mode.json
*.out
# engine state dumps (generated each cycle — never source)
governor_state.json
allocation_state.json
rebalance_state.json
learned_weights.json
stopped_positions.json
decisions.jsonl

# runtime (per-machine / per-session)
harness.pid
*.pid
subscriptions.json
